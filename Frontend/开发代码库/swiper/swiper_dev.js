// swiper二次开发
// console.log(swiperName);
// console.log(swiperName.classNames);
if (swiperName.classNames.length > 0) { // 只有当前页面上存在该swiper时才执行后续代码，防止报错。只有当前页面上存在该swiper时swiper的classNames属性才不为空，否则swiper的classNames属性是一个空的数组。


}
